using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class EventListener : MonoBehaviour
{
    //public enum EventTypes
    //{
    //    OnGameStarted,
    //    OnGameWin,
    //    OnGameLost,
    //    OnGameRestarted,
    //    OnNewLevelLoaded
    //}

    //public EventTypes EventType;

    //public UnityEvent Event;

    //public void Start()
    //{
    //    if (EventType == EventTypes.OnGameStarted)
    //    {
    //        GameController.Instance.OnGameStarted.AddListener(Event.Invoke);
    //    }
    //    else if (EventType == EventTypes.OnGameStarted)
    //    {
    //        GameController.Instance.OnGameStarted.AddListener(Event.Invoke);
    //    }
    //    else if (EventType == EventTypes.OnGameWin)
    //    {
    //        GameController.Instance.OnGameWin.AddListener(Event.Invoke);

    //    }
    //    else if (EventType == EventTypes.OnGameLost)
    //    {
    //        GameController.Instance.OnGameLost.AddListener(Event.Invoke);
    //    }
    //    else if (EventType == EventTypes.OnGameRestarted)
    //    {
    //        GameController.Instance.OnGameRestarted.AddListener(Event.Invoke);
    //    }
    //    else if (EventType == EventTypes.OnNewLevelLoaded)
    //    {
    //        GameController.Instance.OnNewLevelLoaded.AddListener(Event.Invoke);
    //    }

    //}





}
